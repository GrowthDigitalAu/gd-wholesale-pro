query RunInput {
  cart {
    buyerIdentity {
      customer {
        hasAnyTag(tags: ["B2B_approved"])
      }
    }
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
          currencyCode
        }
      }
      merchandise {
        ... on ProductVariant {
          id
          metafield(namespace: "$app", key: "gd_b2b_price") {
            value
          }
        }
      }
    }
  }
}
